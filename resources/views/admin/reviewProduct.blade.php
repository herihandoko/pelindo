<div class="order-item d-flex order_item">
    <img class="order-item-image" src="{{asset(MediaImage(@$product->product->image, 1))}}">
    <div class="order-item-name">
        <b>{{@$product->product->name}}</b>
    </div>
</div>
